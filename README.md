# LLM From Scratch (NumPy + PyTorch)

A pedagogical project to build a Transformer-based language model from first principles.

The goal is to deeply understand how modern LLMs work by implementing every core component manually in NumPy, then rebuilding it in PyTorch for scalability.

